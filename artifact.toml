kind = "web"
previewPath = "/"
title = "Decoscape — Interior Design"
version = "1.0.0"
id = "artifacts/decoscape"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21417

[services.development]
run = "pnpm --filter @workspace/decoscape run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/decoscape", "run", "build" ]
publicDir = "artifacts/decoscape/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21417"
BASE_PATH = "/"
